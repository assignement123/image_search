# image_search
# khoi tao moi truong 
docker-compose up -d

# Đánh giá dataset leave_data (hoặc leaf_data)
python leave_data_analysis.py --path /absolute/path/to/leave_data.csv
python leave_data_analysis.py --path /absolute/path/to/leave_data.csv --json
